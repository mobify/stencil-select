<div class="c-select c--label">
    <label class="c-select__label">{label}</label>
    <span class="c-select__value"></span>
    <select class="js-linked-element" data-linked-element=".{linkedClass}">
        {#items}
            {.}
        {/items}
    </select>
    <div hidden>
        {desktop}
    </div>
</div>
